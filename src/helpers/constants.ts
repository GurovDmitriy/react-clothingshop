export const actionStatusTypes = {
  useless: "useless",
  pending: "pending",
  success: "success",
  failure: "failure",
}

export const cartOperationTypes = {
  increment: "increment",
  decrement: "decrement",
  delete: "delete",
  clear: "clear",
}

export enum ActionStatus {
  useless = "useless",
  pending = "pending",
  success = "success",
  failure = "failure",
}

export enum CartOperation {
  increment = "increment",
  decrement = "decrement",
  delete = "delete",
  clear = "clear",
}
